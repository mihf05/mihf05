version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "${PORT:-8001}:8001"
    environment:
      - DATABASE_URL=${DATABASE_URL:-postgresql://user_lcdajasdhr:sOmqrAFn9q4N6msOvAb4@devinapps-backend-prod.cluster-clussqewa0rh.us-west-2.rds.amazonaws.com/db_bkfgnsyihz?sslmode=require}
      - SECRET_KEY=${SECRET_KEY:-fahimimran0088@gmail.com}
      - API_V1_STR=/api/v1
      - PROJECT_NAME=Employee Management System
      - VERSION=1.0.0
      - ACCESS_TOKEN_EXPIRE_MINUTES=30
      - FIRST_SUPERUSER_EMAIL=admin@example.com
      - FIRST_SUPERUSER_PASSWORD=admin123
      - CORS_ORIGINS=${CORS_ORIGINS:-https://remarkable-dieffenbachia-df2ca5.netlify.app}
      - PORT=8001
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped
    platform: linux/amd64

volumes:
  postgres_data:
